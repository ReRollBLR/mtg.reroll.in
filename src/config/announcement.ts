/**
 * Site-wide announcement banner config.
 * Set `enabled: false` to hide it.
 */
export const announcement = {
  enabled: false,
  text: "1st August : Join us a for a special Secrets of Strixhaven booster draft night. Each player will open a pack pick a card and pass the rest of the pack along. Draft a total of 3 packs of cards and then build a deck on the spot with those cards. Finish the event by playing 3 best of 3 matches to decide the winners! First and second place winners will receive packs as prizes! ",
  ctaText: "Sign up",
  ctaHref: "/tix",
} as const;
